#!/usr/bin/env bash
set -e

# Runs the front-end tests
#
# Usage:
#     ui-tests

# Load shared utils
. ./hg-circle-scripts/utils

log "Installing yarn"
npm install -g yarn

log "Installing dependencies"
yarn  # Install dependencies first

log "Running ESLinter"
yarn run eslint .

log "Running tests"
yarn test
